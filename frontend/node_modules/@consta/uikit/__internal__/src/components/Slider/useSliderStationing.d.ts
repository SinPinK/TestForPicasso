import React from 'react';
import { Line, PropView } from './helper';
declare type UseSliderStationing = (value: number | [number, number] | undefined, min: number, max: number, view: PropView, range: boolean | undefined, step: number | number[] | undefined, buttonRefs: [
    React.RefObject<HTMLButtonElement>,
    React.RefObject<HTMLButtonElement>
], sliderLineRef: React.RefObject<HTMLButtonElement | HTMLDivElement>) => {
    lineSizes: Line[];
    buttonPositions: number[];
};
export declare const getSteps: (step: number | number[], min: number, max: number) => {
    min: number;
    max: number;
}[];
export declare const useSliderStationing: UseSliderStationing;
export {};
